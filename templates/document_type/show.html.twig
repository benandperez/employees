{% extends 'base.html.twig' %}

{% block title %}DocumentType{% endblock %}

{% block body %}
    <h1>DocumentType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ document_type.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ document_type.description }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ document_type.createdAt ? document_type.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ document_type.updatedAt ? document_type.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('document_type_index') }}">back to list</a>

    <a href="{{ path('document_type_edit', {'id': document_type.id}) }}">edit</a>

    {{ include('document_type/_delete_form.html.twig') }}
{% endblock %}
