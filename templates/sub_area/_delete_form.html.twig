<form method="post" action="{{ path('sub_area_delete', {'id': sub_area.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sub_area.id) }}">
    <button class="btn">Delete</button>
</form>
